{
  "name": "auth-microservice",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "prebuild": "npm rebuild bcrypt --build-from-source && yarn clean",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.15",
    "bcrypt": "^5.1.1",
    "fastify": "^5.0.0",
    "http-status-codes": "^2.3.0",
    "joi": "^17.13.3",
    "mongoose": "^8.8.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.16",
    "@types/bcrypt": "^5.0.2",
    "@types/uuid": "^10.0.0"
  }
}
